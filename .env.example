# Copy this file to .env and paste your real key (see README / Appendix A of the build plan).
# Get a free key at https://aistudio.google.com — "Get API key" → "Create API key".
GEMINI_API_KEY=your_key_here

# Optional: override the model (defaults to gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash
